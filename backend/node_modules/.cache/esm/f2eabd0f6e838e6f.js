let fs,express,swaggerUi,bodyParser,cors,InputError,AccessError,swaggerDocument,getEmailFromAuthorization,login,logout,register,save,listingLength,addPilot,getAllListings,getDetails,assertOwnsListing,updateIncrementListing,updateDecrementListing,removeListing,getTimeValue;_6e8‍.x([["default",()=>_6e8‍.o]]);_6e8‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_6e8‍.w("express",[["default",["express"],function(v){express=v}]]);_6e8‍.w("swagger-ui-express",[["default",["swaggerUi"],function(v){swaggerUi=v}]]);_6e8‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_6e8‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_6e8‍.w("./error",[["InputError",["InputError"],function(v){InputError=v}],["AccessError",["AccessError"],function(v){AccessError=v}]]);_6e8‍.w("../swagger.json",[["default",["swaggerDocument"],function(v){swaggerDocument=v}]]);_6e8‍.w("./service",[["getEmailFromAuthorization",["getEmailFromAuthorization"],function(v){getEmailFromAuthorization=v}],["login",["login"],function(v){login=v}],["logout",["logout"],function(v){logout=v}],["register",["register"],function(v){register=v}],["save",["save"],function(v){save=v}],["listingLength",["listingLength"],function(v){listingLength=v}],["addPilot",["addPilot"],function(v){addPilot=v}],["getAllListings",["getAllListings"],function(v){getAllListings=v}],["getDetails",["getDetails"],function(v){getDetails=v}],["assertOwnsListing",["assertOwnsListing"],function(v){assertOwnsListing=v}],["updateIncrementListing",["updateIncrementListing"],function(v){updateIncrementListing=v}],["updateDecrementListing",["updateDecrementListing"],function(v){updateDecrementListing=v}],["removeListing",["removeListing"],function(v){removeListing=v}],["getTimeValue",["getTimeValue"],function(v){getTimeValue=v}]]);
























const app = express();

app.use(cors());
app.use(bodyParser.urlencoded({ extended: true }));
app.use(bodyParser.json({ limit: '50mb' }));

const catchErrors = (fn) => async (req, res) => {
  try {
    await fn(req, res);
    save();
  } catch (err) {
    if (err instanceof InputError) {
      res.status(400).send({ error: err.message });
    } else if (err instanceof AccessError) {
      res.status(403).send({ error: err.message });
    } else {
      res.status(500).send({ error: 'A system error ocurred' });
    }
  }
};

/***************************************************************
                       User Auth Functions
***************************************************************/

const authed = (fn) => async (req, res) => {
  const email = getEmailFromAuthorization(req.header('Authorization'));
  await fn(req, res, email);
};

app.post(
  '/user/auth/login',
  catchErrors(async (req, res) => {
    const { email, password, currMonth } = req.body;
    const token = await login(email, password, currMonth);
    const timeV = await getTimeValue(email);
    return res.json({ token, timeV });
  }),
);

app.post(
  '/user/auth/register',
  catchErrors(async (req, res) => {
    const { email, password, name, time, date } = req.body;
    const token = await register(email, password, name, time, date);
    return res.json({ token });
  }),
);

app.post(
  '/user/auth/logout',
  catchErrors(
    authed(async (req, res, email) => {
      await logout(email);
      return res.json({});
    }),
  ),
);

/***************************************************************
                       Listing Functions
***************************************************************/
app.post(
  '/listings/new',
  catchErrors(
    authed(async (req, res, email) => {
      const { name, patternNumber, monthlyTime, quaterTime, yearlyTime, thumbnail } = req.body;
      return res.status(200).json({
        listingId: await addPilot(name, email, patternNumber, monthlyTime, quaterTime, yearlyTime, thumbnail),
        listingLen: await listingLength(),
      });
    }),
  ),
);

app.put(
  '/listings/eddit/:listingid',
  catchErrors(
    authed(async (req, res, email) => {
      const { listingid } = req.params;
      const { time, month, symbol } = req.body;
      await assertOwnsListing(email, listingid);
      if (symbol === '+') {
        await updateIncrementListing(listingid, time, month);
      } else {
        await updateDecrementListing(listingid, time, month);
      }
      return res.status(200).send({});
    }),
  ),
);

app.delete(
  '/listings/remove/:listingid',
  catchErrors(
    authed(async (req, res, email) => {
      const { listingid } = req.params;
      await assertOwnsListing(email, listingid);
      await removeListing(listingid);
      return res.status(200).send({});
    }),
  ),
);

app.get(
  '/listings/detail/:listingid',
  catchErrors(
    authed(async (req, res, email) => {
      const { listingid } = req.params;
      return res.status(200).send({
        listings: await getDetails(listingid)
      });
    }),
  ),
)
/***************************************************************
                       Homepage Functions
***************************************************************/
app.get(
  '/listings',
  catchErrors(async (req, res) => {
    return res.json({ listings: await getAllListings() });
  }),
);

/***************************************************************
                       Running Server
***************************************************************/

app.get('/', (req, res) => res.redirect('/docs'));

app.use('/docs', swaggerUi.serve, swaggerUi.setup(swaggerDocument));

const configData = JSON.parse(fs.readFileSync('../frontend/src/config.json'));
const port = 'BACKEND_PORT' in configData ? configData.BACKEND_PORT : 5033;

const server = app.listen(port, () => {
  console.log(`Backend is now listening on port ${port}!`);
  console.log(`For API docs, navigate to http://localhost:${port}`);
});

_6e8‍.d(server);
